{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && cpx \"../playground/dist/**\" dist && cpx \"../docs/dist/docs/**\" dist"
  },
  "dependencies": {
    "docs": "workspace:*",
    "playground": "workspace:*"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^6.1.0"
  }
}
